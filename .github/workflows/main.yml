name: First workflow
on: [workflow_dispatch,push]

jobs:
  Greeting:
    runs-on: ubuntu-latest
    steps:
      - name : say hello
        run: echo "hello world"
      - name : print context
        run : echo "${{toJSON(github)}}"
